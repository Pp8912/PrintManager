<Window x:Class="PrintManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrintManager"
        mc:Ignorable="d"
        Title="Print Manager - Conversión de Color" Height="600" Width="900" 
        WindowStartupLocation="CenterScreen" Background="#F0F0F0">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <!-- Preview Area -->
        <Border Grid.Column="0" Margin="10" Background="Gray" CornerRadius="5">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Image x:Name="PreviewImage" Stretch="Uniform" Margin="20"/>
            </ScrollViewer>
        </Border>

        <!-- Control Panel -->
        <Border Grid.Column="1" Background="White" Margin="0,10,10,10" CornerRadius="5" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="#CCCCCC" ShadowDepth="2"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="AJUSTES DE IMPRESIÓN" FontWeight="Bold" FontSize="16" Margin="0,0,0,20" Foreground="#333"/>

                <TextBlock Text="Impresora Física:" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <ComboBox x:Name="PrinterSelector" Height="30" Padding="5"/>

                <!-- Selector de Color -->
                <TextBlock Text="Conversión de Color:" FontWeight="SemiBold" Margin="0,20,0,5"/>
                <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="10" Background="#F9F9F9">
                    <StackPanel>
                        <ComboBox x:Name="ColorSelector" Height="32" Padding="5"
                                  SelectedIndex="1">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Width="18" Height="18" CornerRadius="3" 
                                                BorderBrush="#999" BorderThickness="1" Margin="0,0,8,0"
                                                Background="{Binding DisplayBrush}"/>
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontSize="13"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <TextBlock Text="Sustituye textos y gráficos negros por el color seleccionado." 
                                   TextWrapping="Wrap" FontSize="11" Foreground="#666" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Orientación de Página -->
                <TextBlock Text="Documento:" FontWeight="SemiBold" Margin="0,20,0,5"/>
                <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="10" Background="#F9F9F9">
                    <StackPanel>
                        <TextBlock x:Name="OrientationText" Text="📐 Orientación: —" FontSize="13" Foreground="#333"/>
                        <TextBlock x:Name="PageSizeText" Text="📄 Tamaño: —" FontSize="13" Foreground="#555" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Button x:Name="OpenButton" Content="📂 Abrir Otro PDF..." Height="30" Margin="0,15,0,0" Background="#EEE" Foreground="#333" BorderBrush="#CCC" BorderThickness="1" Cursor="Hand" Click="OpenButton_Click">
                     <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Separator Margin="0,20"/>

                <TextBlock x:Name="StatusText" Text="Listo" Foreground="Gray" TextAlignment="Center" Margin="0,10"/>

                <Button x:Name="PrintButton" Content="IMPRIMIR" Height="50" FontSize="16" FontWeight="Bold" Background="#00AEEF" Foreground="White" Margin="0,10,0,0" BorderThickness="0" Cursor="Hand" Click="PrintButton_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button x:Name="CancelButton" Content="Cancelar" Height="35" Margin="0,10,0,0" Background="Transparent" BorderBrush="#999" BorderThickness="1" Foreground="#666" Cursor="Hand" Click="CancelButton_Click">
                     <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</Window>
